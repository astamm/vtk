#!/bin/sh
# cleanup.win -- remove generated files on Windows

rm -f inst/vtk.conf
# Remove DLLs copied from a shared system VTK installation.
# DLLs from the pre-built shared download live in inst/windows/ and are
# removed when that directory is cleaned by the installer.
rm -rf inst/libs

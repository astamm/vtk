% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_rvtk.R
\name{use_rvtk}
\alias{use_rvtk}
\title{Set up a downstream package to use rvtk}
\usage{
use_rvtk(
  modules = c("vtkIOLegacy", "vtkIOXML", "vtkIOXMLParser", "vtkIOCore", "vtkCommonCore",
    "vtkCommonDataModel", "vtkCommonExecutionModel", "vtkCommonMath", "vtkCommonMisc",
    "vtkCommonSystem", "vtkCommonTransforms", "vtksys"),
  path = "."
)
}
\arguments{
\item{modules}{A character vector of VTK module names to link against.
These are passed to \code{\link[=LdFlagsFile]{LdFlagsFile()}} in the generated \code{tools/configure.R},
restricting linking to only the modules the downstream package needs.
Defaults to a standard set covering common I/O and core modules
(the same set used by the reference implementation in
\url{https://github.com/astamm/riot}).}

\item{path}{Path to the root of the downstream package. Defaults to the
current working directory.}
}
\value{
Invisibly, the normalised path to the package root. Called
primarily for its side effects.
}
\description{
A \href{https://usethis.r-lib.org/}{usethis}-style helper that configures a
downstream R package to link against VTK via \strong{rvtk}. It performs the
following steps:
\itemize{
\item Adds \code{rvtk} to the \code{Imports} field of \code{DESCRIPTION}.
\item Writes \code{src/Makevars} that queries compiler and linker flags at install
time by calling \code{tools/configure.R}.
\item Writes \code{src/Makevars.win} with the Windows-specific \verb{$(shell ...)} syntax
that does the same.
\item Writes \code{tools/configure.R} that calls \code{\link[=CppFlags]{CppFlags()}} and \code{\link[=LdFlagsFile]{LdFlagsFile()}}
with the requested VTK modules.
\item Adds \code{src/vtk_libs.rsp} to \code{.gitignore} (it is generated at install time
and must not be committed).
\item Creates \code{R/rvtk_imports.R} with a minimal \verb{@importFrom rvtk} roxygen tag
so that \verb{R CMD check} does not complain about \strong{rvtk} being listed in
\code{Imports} without any function import in the R code.
}

After running \code{use_rvtk()} the downstream package is fully configured: VTK
compiler and linker flags are resolved automatically at \verb{R CMD INSTALL} time
on all platforms without any shell \code{configure} / \code{configure.win} scripts.
}
\seealso{
\code{\link[=CppFlags]{CppFlags()}}, \code{\link[=LdFlagsFile]{LdFlagsFile()}}
}
